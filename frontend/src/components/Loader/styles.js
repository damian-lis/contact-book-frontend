import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  authLoader: {
    color: 'white',
    opacity: 0.6,
    width: '24px',
    height: '24px'
  },

  tableLoader: {
    color: 'blue'
  }
}));
