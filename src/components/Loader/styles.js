import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  authLoader: {
    color: 'white',
    opacity: 0.6,
    width: 24,
    height: 24
  },

  tableLoader: {
    color: 'blue'
  }
}));
